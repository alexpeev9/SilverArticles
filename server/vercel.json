{
  "version": 2,
  "public": true,
  "installCommand": "npm install && cd ../server && npm install && npm run build",
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/src/index.ts"
    }
  ]
}
